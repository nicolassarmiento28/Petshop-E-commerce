NODE_ENV=
PORT=
DATABASE_URL=
# En producción debe ser un valor largo y aleatorio (ej. openssl rand -hex 32) — nunca reutilizar este ejemplo
JWT_SECRET=
# Sesión admin corta porque el token vive en localStorage (sin cookie httpOnly)
JWT_EXPIRES_IN=8h
FRONTEND_URL=
RETURN_URL=
VET_RETURN_URL=
TBK_COMMERCE_CODE=
TBK_API_KEY=
RESEND_API_KEY=
EMAIL_FROM=


